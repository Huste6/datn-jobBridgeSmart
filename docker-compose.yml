services:
  mongodb:
    image: mongo:7.0
    container_name: jobbridge-mongodb
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
